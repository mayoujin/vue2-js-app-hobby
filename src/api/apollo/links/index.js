/**
 * Links compilation point.
 * HttpLink, ErrorLink, AuthLink etc. are connected here.
 */
import { from as fromArray } from 'apollo-boost'
//import httpLink from './http'

export default fromArray([])
